import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const devices=sqliteTable('devices',{id:text('id').primaryKey(),settings:text('settings').notNull(),subscription:text('subscription'),revision:integer('revision').notNull().default(0),updated:integer('updated').notNull()});
export const kv=sqliteTable('kv',{key:text('key').primaryKey(),value:text('value').notNull(),updated:integer('updated').notNull()});
export const sessions=sqliteTable('sessions',{id:text('id').primaryKey(),device:text('device').notNull(),kind:text('kind').notNull(),started:integer('started').notNull(),ends:integer('ends').notNull(),duration:integer('duration').notNull(),elapsed:integer('elapsed').notNull().default(0),status:text('status').notNull(),day:text('day').notNull()},t=>[index('sessions_device_day').on(t.device,t.day)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),device:text('device').notNull(),group:text('group_id').notNull(),revision:integer('revision').notNull(),due:integer('due').notNull(),expires:integer('expires').notNull(),payload:text('payload').notNull(),status:text('status').notNull().default('pending'),attempts:integer('attempts').notNull().default(0),lease:integer('lease').notNull().default(0),sent:integer('sent'),delivered:integer('delivered'),error:text('error')},t=>[index('events_due_status').on(t.status,t.due),index('events_device_group').on(t.device,t.group)]);
